---
"@nielpattin/pi-subagents": minor
---

Add the built-in omni visual inspection agent, improve get_subagent_result expanded rendering to show complete results without the misleading verbose path, and clarify background-result guidance so agents do not block on `wait: true` unless the user asked to wait.
