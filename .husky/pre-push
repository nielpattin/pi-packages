# Git exports local repository variables into hooks. This repo is used as a
# submodule checkout, and the test suite creates temporary Git repositories.
# Clear the parent repo environment so nested Git commands operate on their cwd.
unset $(git rev-parse --local-env-vars)

pnpm test
node scripts/require-changeset.mjs origin/main
